[project]
name = "donate-hub"
version = "0.1.0"
description = "DonateHub - A platform for connecting donors with causes."
dependencies = [
    "django",
    "django-allauth",
    "djangorestframework",
    "djangorestframework-simplejwt",
    "django-cors-headers",
    "python-dotenv",
    "psycopg2-binary",
    "google-generativeai",
    "xhtml2pdf",
    "gunicorn",
    "whitenoise",
]

[project.scripts]
app = "donatehub.wsgi:application"

[tool.vercel]
# Optional Vercel settings if needed.
